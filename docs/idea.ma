1. трейлінг-стоп.
2. yaml
3. один ордер відкритий



Рекомендовані наступні кроки (порядок)
1. Запустити pytest і прислати лог, якщо є помилки — виправлю.
2. Перевірити/адаптувати виклики до реального Binance-клієнта (я можу автоматично підлаштувати під вашу бібліотеку).
3. Пройтись по configs і задати безпечні значення ризику.
4. Додати mock/integration тест для бекетесту з невеликою серією свічок.
5. Налаштувати CI secrets і додати coverage + mypy.

Хочеш, щоб зараз:
- A) я запустив пошук всіх calculate_sl_tp викликів і згенерував diff змін (де треба підставити fee_pct),
- B) я адаптував виклики під python-binance (вкажи версію бібліотеки),
- C) я прогнав pytest локально і прислав помилки/фікси (надаси логи)?

Вкажи варіант (A/B/C) — застосую.Рекомендовані наступні кроки (порядок)
1. Запустити pytest і прислати лог, якщо є помилки — виправлю.
2. Перевірити/адаптувати виклики до реального Binance-клієнта (я можу автоматично підлаштувати під вашу бібліотеку).
3. Пройтись по configs і задати безпечні значення ризику.
4. Додати mock/integration тест для бекетесту з невеликою серією свічок.
5. Налаштувати CI secrets і додати coverage + mypy.

Хочеш, щоб зараз:
- A) я запустив пошук всіх calculate_sl_tp викликів і згенерував diff змін (де треба підставити fee_pct),
- B) я адаптував виклики під python-binance (вкажи версію бібліотеки),
- C) я прогнав pytest локально і прислав помилки/фікси (надаси логи)?

Вкажи варіант (A/B/C) — застосую.